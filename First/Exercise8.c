#include <stdio.h>

int main()
{
     char chr;
     printf("Enter a character : ");
     scanf("%c", &chr);
     
     //Displays the character entered when %c is used
     printf("You entered %c.\n", chr);
     
     //Displays the ASCII value of the character when %d is used
     printf("ASCII value is %d. , chr"):
     return 0;
}
